name: brew test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Add Homebrew to PATH (Linux)
        if: runner.os == 'Linux'
        run: echo "/home/linuxbrew/.linuxbrew/bin" >> "$GITHUB_PATH"

      # Runners ship with HOMEBREW_NO_AUTO_UPDATE=1 and a preinstalled Homebrew
      # that goes stale. Core formulae (ca-certificates) then use install-step
      # syntax the bundled Homebrew cannot parse: "unknown install step: run".
      - name: Update Homebrew
        run: brew update

      - name: Tap this checkout
        run: brew tap pavel-gorlov/tap "$GITHUB_WORKSPACE"

      - name: Install from source
        run: brew install --build-from-source pavel-gorlov/tap/ai-dotfiles

      - name: Test formula
        run: brew test pavel-gorlov/tap/ai-dotfiles

      - name: Audit formula
        run: brew audit --strict pavel-gorlov/tap/ai-dotfiles

      - name: Smoke run
        run: ai-dotfiles --version
